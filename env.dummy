DB_CONFIG = {
    "user": os.getenv("DB_USERNAME", "sunsin"),
    "dbname" : st.secrets["db_name"],
    "password" : st.secrets["db_password"],
    "host" : st.secrets["db_host"],
    "port" : st.secrets["db_port"]
}
